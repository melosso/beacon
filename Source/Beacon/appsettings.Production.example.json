{
  "Serilog": {
    "Using": ["Serilog.Sinks.Console", "Serilog.Sinks.File"],
    "___comment_auth_noise___": "Beacon.Security.*AuthHandler are held at Warning: AuthenticationHandler logs 'scheme was challenged' at Information under the handler's own category, not Microsoft.*, so it escapes the Microsoft override and fires on every unauthenticated request.",
    "MinimumLevel": {
      "Default": "Warning",
      "Override": {
        "Microsoft": "Warning",
        "System": "Warning",
        "Serilog.AspNetCore": "Warning",
        "Beacon.Security.ApiKeyAuthHandler": "Warning",
        "Beacon.Security.JwtAuthHandler": "Warning"
      }
    },
    "WriteTo": [
      {
        "Name": "File",
        "Args": {
          "path": "log/beacon-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 14,
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss} [{Level:u3}] {Message:lj}{NewLine}{Exception}"
        }
      },
      {
        "Name": "Console",
        "Args": {
          "outputTemplate": "{Timestamp:HH:mm:ss} [{Level:u3}] {Message:lj}{NewLine}{Exception}"
        }
      }
    ]
  },
  "AllowedHosts": "beacon-api.example.com;beacon-admin.example.com",
  "Beacon": {
    "TokenExpiryDays": 7,
    "DatabaseProvider": "sqlite",

    "___comment_encryption___": "Generate secure random values for production. These will be encrypted on first run.",
    "ConnectionString": "Data Source=Beacon.db",
    "SigningKey": "your-secure-signing-key-here",
    "EncryptionKey": "your-secure-encryption-key-here",
    "Pepper": "your-secure-pepper-here",
    "AdminApiKey": "your-secure-api-key-here",

    "___comment_routing___": "Host-based routing (leave empty for port-based mode)",
    "ApiHosts": "beacon-api.example.com",
    "AdminHosts": "beacon-admin.example.com",
    "PublicUrl": "https://beacon-api.example.com",
    "AllowedOrigins": "https://app.example.com,https://dashboard.example.com",

    "___comment_ports___": "Port-based routing (used when ApiHosts/AdminHosts are empty)",
    "ApiPort": 5000,
    "AdminPort": 5001,

    "___comment_proxy___": "Enable when behind a reverse proxy (nginx, traefik, etc.)",
    "TrustForwardedHeaders": true,

    "___comment_https___": "Enable to redirect HTTP to HTTPS and set HSTS headers. Only set if Beacon terminates TLS directly, leave false when a reverse proxy handles SSL.",
    "EnforceHttps": false,

    "___comment_instance___": "Instance-level feature flags (not editable via the admin UI)",
    "DisableEmailNotifications": false
  }
}
